naftiko: 1.0.0-alpha2
info:
  label: Logto API references — Captcha provider
  description: 'Logto API references — Captcha provider. 3 operations. Lead operation: Get captcha provider. Self-contained
    Naftiko capability covering one Logto business surface.'
  tags:
  - Logto
  - Captcha provider
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    LOGTO_API_KEY: LOGTO_API_KEY
capability:
  consumes:
  - type: http
    namespace: logto-captcha-provider
    baseUri: https://[tenant_id].logto.app
    description: Logto API references — Captcha provider business capability. Self-contained, no shared references.
    resources:
    - name: api-captcha-provider
      path: /api/captcha-provider
      operations:
      - name: getcaptchaprovider
        method: GET
        description: Get captcha provider
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: updatecaptchaprovider
        method: PUT
        description: Update captcha provider
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deletecaptchaprovider
        method: DELETE
        description: Delete captcha provider
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.LOGTO_API_KEY}}'
  exposes:
  - type: rest
    namespace: logto-captcha-provider-rest
    port: 8080
    description: REST adapter for Logto API references — Captcha provider. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/captcha-provider
      name: api-captcha-provider
      description: REST surface for api-captcha-provider.
      operations:
      - method: GET
        name: getcaptchaprovider
        description: Get captcha provider
        call: logto-captcha-provider.getcaptchaprovider
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: updatecaptchaprovider
        description: Update captcha provider
        call: logto-captcha-provider.updatecaptchaprovider
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deletecaptchaprovider
        description: Delete captcha provider
        call: logto-captcha-provider.deletecaptchaprovider
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: logto-captcha-provider-mcp
    port: 9090
    transport: http
    description: MCP adapter for Logto API references — Captcha provider. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: get-captcha-provider
      description: Get captcha provider
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: logto-captcha-provider.getcaptchaprovider
      outputParameters:
      - type: object
        mapping: $.
    - name: update-captcha-provider
      description: Update captcha provider
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: logto-captcha-provider.updatecaptchaprovider
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: delete-captcha-provider
      description: Delete captcha provider
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: logto-captcha-provider.deletecaptchaprovider
      outputParameters:
      - type: object
        mapping: $.
