{
  "opencollection": "1.0.0",
  "info": {
    "name": "Logto API references Account center Subject tokens API",
    "version": "Cloud"
  },
  "request": {
    "auth": {
      "type": "oauth2",
      "flow": "client_credentials",
      "accessTokenUrl": "/oidc/token",
      "credentials": {
        "clientId": "{{clientId}}",
        "clientSecret": "{{clientSecret}}"
      }
    }
  },
  "items": [
    {
      "info": {
        "name": "Subject tokens",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Create a new subject token.",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://[tenant_id].logto.app/api/subject-tokens",
            "body": {
              "type": "json",
              "data": "{}"
            }
          },
          "docs": "Create a new subject token for the use of impersonating the user."
        }
      ]
    }
  ],
  "bundled": true
}